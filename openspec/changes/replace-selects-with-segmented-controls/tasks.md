## 1. Estilos Globales para Controles Segmentados

- [x] 1.1 Agregar reglas de estilo CSS para `.control-segmentado` y botones `.activo` en `src/app.css`.

## 2. Actualización de Componentes Svelte

- [x] 2.1 Sustituir los elementos `<select>` de tipo, moneda y filtros en `src/lib/components/TransactionsView.svelte` por controles segmentados.
- [x] 2.2 Sustituir los elementos `<select>` de dirección y moneda (abonos) en `src/lib/components/DebtsView.svelte` por controles segmentados.
- [x] 2.3 Sustituir el elemento `<select>` de moneda (pagos) en `src/lib/components/FixedExpensesView.svelte` por controles segmentados.

## 3. Verificación y Pruebas

- [x] 3.1 Ejecutar la suite de pruebas unitarias (`npm test`) para garantizar la preservación de la funcionalidad.
